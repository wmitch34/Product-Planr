# Product Planr API

Local Node/Express/SQLite API for Product Planr.

```sh
npm install
npm run dev
```

The API listens on `http://localhost:3001`. Set `PORT` or `DATABASE_PATH` to override defaults.
